# AiNewS 環境変数設定例
# このファイルをコピーして .env として保存し、必要に応じて値を変更してください。
# .env は .gitignore に含まれており、リポジトリにはコミットされません。

# アプリの待ち受けポート番号（デフォルト: 5000）
PORT=5000

# デバッグモードの有効化（1=有効, 0=無効）
# 本番環境では必ず 0 に設定してください。
FLASK_DEBUG=0
